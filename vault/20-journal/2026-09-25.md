# 2026-09-25

- Examined upstream `latest` (6293630a84): three static sweeps (MIP hot paths, LA/simplex, presolve/IO) → 31 candidates
  in [[Candidates]]. HiGHS CONTRIBUTING: no AI-generated solver PRs → issues with evidence only.
- Fork Tideseed/HiGHS (public), lab repo Tideseed/highs-lab (public). Berk: quick failures over exhaustive tests →
  `bench/screen.py` (waves of 10, kill on wrong answer / identical-search break / ratio > 1.02).
- Window 11:00–15:45 CEST: both local LLM servers stopped (Berk OK), perf_event_paranoid temporarily 4 → 2.
- Builds: main, dev, dev-prof, dev-test (101/101 tests pass).
- **s100 (#3314) reproduced on latest**: 172 s for a 60 s limit, 0 nodes. perf: 90 % in
  `HighsCliqueTable::cliquePartition` pairwise `findCommonCliqueId` (not the analytic-centre IPM the static read
  suspected). Fix on `lab/clique-partition-marking`: 62 s, feasible solution, gap 57 %. First version used a count
  threshold and fell back to pairwise on s100's ~5000-cliques; a cost-based threshold (16x) fixed that.
- 11:20–13:00 profile sweeps (12 mixed + 16 hard instances, 60 s each) → targets by measured share:
  DSE weight recompute (C5, up to 27 %), clique queries (C1, sorrell3 44 %), clique merging (C2, chromaticindex 52 %),
  symmetry hash (C3), presolve column walks (P1 17 %, C7 13 % on toguru), unused glpsol errors (C6, 1–2 % everywhere).
- Generalising C1 into queryNeighbourhood crashed doon (heap corruption). A shadow build comparing marking vs pairwise
  on every query found: (1) haveCommonClique is false for the same column, (2) HighsHashTree::find_common misses keys
  when leaves hold several entries with equal hashes — an upstream bug (C4, lab/hash-tree-find-common). With both,
  0 mismatches on 10 instances.
- lab/dse-carry-weights: cache DSE weights by basis hash (end of solve + before basis replacement) and carry them
  across cut rows. krka solved to optimality in ~20 s.
- All four code branches: 101/101 unit tests. Agent Fast restarted at 12:27 on Berk's request (bench continues with it
  up; its model load overlapped the start of r4 → noise, noted). Berk: full benchmarks at night.
